# Backend-specific patterns (if any)
# All common patterns are managed in the root .gitignore file
# This file is kept for potential backend-specific ignores in the future

# Backend-specific directories (if needed)
# Example:
# dist/
# build/
